# Louis "Baboon" Kottmann's Bash configuration

## Installation

Simple clone this repo into `~/`

## Updating it

If you add a new file to it (ex: `.profile`), remember to add it to the whitelist in `.gitignore`
